[package]
name = "workspace"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4.6", features = ["derive"] }
reqwest = { version = "0.11.22", features = ["stream", "blocking", "json"] }
serde_json = "1.0.107"
